# Error Log

오류 해결 및 작업 방식 교정 이력.

---

## 2026-04-15

**판단 실수**: result.tsx의 `/api/winner` 동작을 코드를 읽지 않고 "의도한 대로 구현되어 있다"고 단정했다. 실제로는 전체 1위가 아닌 가장 최근 플레이 기록을 반환하고 있었다.

**교정 방향**: "확인했다"는 표현은 해당 코드를 직접 읽고 검증한 경우에만 사용한다.

## 2026-04-16

**판단 실수**: 배열이 닫힌 `];` 이후에 새 항목을 삽입해 `commentTemplates.ts` 구문 오류 발생.

**교정 방향**: 배열·객체에 항목을 추가할 때는 닫는 괄호 위치를 먼저 확인하고 그 안에 삽입한다.

## 2026-05-05

**판단 실수**: result.tsx에서 「優勝回数 컬럼 삭제」 요청을 받았으나, 코드를 확인하지 않고 優勝率까지 함께 삭제했다. 사용자가 명시적으로 남기라고 한 컬럼이었다.

**교정 방향**: 삭제 작업 전 요청 범위와 실제 코드를 대조한 뒤 삭제 대상을 명확히 확인한다. 요청에 없는 항목은 건드리지 않는다.

## 2026-05-05

**판단 실수**: gif+mp4 혼합 게임 수정 페이지에서 `previewUrl`이 undefined인 경우를 고려하지 않아 `.endsWith()` 호출 시 TypeError 크래시 발생.

**교정 방향**: 외부 데이터(DB 조회, 업로드 결과)에서 가져온 문자열 필드는 항상 undefined 가능성을 상정하고 옵셔널 체이닝(`?.`)을 적용한다.

## 2026-04-14

**판단 실수**: 새 필드를 DB에 저장하고 프론트에서 사용하는 코드까지 작성했지만, 그 사이 데이터를 전달하는 매핑 코드를 끝까지 읽지 않고 "문제없다"고 판단했다.

**교정 방향**: 데이터 흐름 전체(저장→전달→사용)를 실제로 코드로 확인한 후에만 완료로 간주한다.
